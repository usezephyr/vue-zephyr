export default interface Locale {
  pages: {
    _global: {
      example: string;
      props: string;
      usage: string;
      variant: string;
      size: string;
    };
    button: {
      title: string;
    };
  };
}
